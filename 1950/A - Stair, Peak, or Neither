#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
	int t;
	cin >> t;
	while(t --)
	{
	    int a, b, c;
	    cin >> a >> b >> c;
	    
	    if(a >= b || b == c) cout << "NONE" << '
';
	    if(a < b && b < c) cout << "STAIR" << '
';
	    if(a < b && b > c) cout << "PEAK" << '
';
	}
	
	return 0;
}