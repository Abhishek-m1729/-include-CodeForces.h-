#include <bits/stdc++.h>
#define endl '
'
using namespace std;
 
int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	int tc;
	cin >> tc;
	while(tc --)
	{
	    string nums;
	    cin >> nums;
	    
	    int sum3f = 0, sum3s = 0;
	    for(int i=0; i<6; ++i){
	        if(i < 3) sum3f += (nums[i] - '0');
	        else sum3s += (nums[i] - '0');
	    }
	    
	    if(sum3f == sum3s) cout << "YES" << endl;
	    else cout << "NO" << endl;
	}
 
    return 0;
}